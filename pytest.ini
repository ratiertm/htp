[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --timeout=60
markers =
    regression: Phase 1-4 회귀 테스트 (기본 동작 보존)
    slow: 30초 이상 걸리는 테스트
filterwarnings =
    ignore::DeprecationWarning
